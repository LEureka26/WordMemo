# WordMemo 技术架构文档

## 1. 系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户界面层 (UI Layer)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │   学习模块   │  │   默写模块   │  │   词库模块   │  │   统计模块   │ │
│  │  WordCard   │  │  QuizInput  │  │  WordList   │  │  StatsView  │ │
│  │  AudioBtn   │  │  ResultHint │  │  GroupMgr   │  │  StreakCard │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────────┐
│                      业务逻辑层 (Logic Layer)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │  Learning   │  │   QuizMgr   │  │  WordStore  │  │   Stats     │ │
│  │   Engine    │  │             │  │             │  │             │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │
│  │   AudioMgr  │  │   Gesture   │  │  KeyHandler │                 │
│  │             │  │   Handler   │  │             │                 │
│  └─────────────┘  └─────────────┘  └─────────────┘                 │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────────┐
│                       数据存储层 (Storage Layer)                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                      localStorage                              │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │  │
│  │  │wordGroups│ │settings │ │ streak  │ │wrongList│ │ wordMap │ │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ │  │
│  └───────────────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────────┐
│                      浏览器能力层 (Browser Layer)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │speechSynth  │  │   Storage   │  │   Network   │  │   Service   │ │
│  │             │  │   API       │  │   API       │  │   Worker    │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

## 2. 模块划分

### 2.1 学习模块 (Learning)

| 子模块 | 职责 | 状态管理 |
|--------|------|----------|
| WordCard | 单词卡片展示、中英文切换 | `currentWord`, `showChinese` |
| AudioBtn | 发音按钮交互 | `isSpeaking` |
| ProgressBar | 学习进度显示 | `progress`, `total` |

### 2.2 默写模块 (Quiz)

| 子模块 | 职责 | 状态管理 |
|--------|------|----------|
| QuizInput | 中文输入框、提交判定 | `userInput`, `isCorrect`, `feedback` |
| ResultHint | 结果反馈动画与提示 | `resultType`, `correctAnswer` |
| WrongCollector | 错题收集与标记 | `wrongList`, `currentWrongCount` |

### 2.3 词库模块 (WordStore)

| 子模块 | 职责 | 状态管理 |
|--------|------|----------|
| WordList | 单词列表展示、筛选、搜索 | `wordList`, `filter`, `searchQuery` |
| GroupMgr | 分组创建、切换、删除 | `groups`, `activeGroup` |
| ImportExport | 导入导出 JSON/CSV | `importFile`, `exportData` |

### 2.4 统计模块 (Stats)

| 子模块 | 职责 | 状态管理 |
|--------|------|----------|
| StatsView | 正确率统计、学习数据可视化 | `statsData`, `chartConfig` |
| StreakCard | 连续打卡天数展示 | `streak`, `badges` |
| DifficultList | 难词本列表 | `difficultWords` |

### 2.5 音频管理模块 (AudioMgr)

| 功能 | 实现 |
|------|------|
| 语音合成 | `window.speechSynthesis` |
| 语音选择 | 优先 `en-US`，备选 `en-GB` |
| 语速调节 | `rate: 0.5/0.9/1.5` |
| 自动播放 | 首次激活后自动朗读 |
| 兼容性处理 | iOS Safari `onvoiceschanged` 轮询 |

### 2.6 手势处理模块 (GestureHandler)

| 手势 | 移动端 | PC端 |
|------|--------|------|
| 左滑 | 上一个单词 | ← 方向键 |
| 右滑 | 下一个单词 | → 方向键 |
| 上滑 | 标记已掌握 | Shift + ↑ |
| 点击卡片 | 发音 | 空格键 |

### 2.7 键盘处理模块 (KeyHandler)

| 快捷键 | 功能 | 适用场景 |
|--------|------|----------|
| ← / → | 切换单词 | 学习模式 |
| Space | 发音 | 学习模式 |
| Enter | 提交默写答案 | 默写模式 |
| Ctrl+Enter | 提交默写答案 | 默写模式 |
| Tab | 切换焦点 | 默写模式 |
| 1/2/3 | 语速调节 | 全局 |
| ? | 快捷键帮助 | 全局 |

## 3. 技术栈选型

| 层次 | 技术 | 版本 | 理由 |
|------|------|------|------|
| 框架 | Vue 3 | 3.4+ | 组合式API、响应式系统、生态成熟 |
| UI组件 | Ant Design Vue | 4.x | 丰富组件库、响应式设计、TypeScript支持 |
| 构建工具 | Vite | 6.x | 快速开发、ESM原生支持、零配置 |
| 状态管理 | Pinia | 2.x | Vue3官方推荐、轻量、类型安全 |
| 路由 | Vue Router | 4.x | SPA路由管理、嵌套路由支持 |
| CSS框架 | Tailwind CSS | 3.x | 原子化CSS、响应式优先、与AntD兼容 |
| 图表 | Chart.js | 4.x | 轻量、移动端友好、进度图表 |
| PWA | Workbox | 7.x | Service Worker生成、缓存策略 |
| 图标 | Lucide Vue | latest | 轻量SVG图标、按需加载 |

## 4. 接口设计

### 4.1 存储接口 (Storage API)

```typescript
interface StorageService {
  getWordGroups(): Promise<Record<string, Word[]>>
  setWordGroups(groups: Record<string, Word[]>): Promise<void>
  
  getActiveGroup(): Promise<string>
  setActiveGroup(group: string): Promise<void>
  
  getSettings(): Promise<Settings>
  setSettings(settings: Settings): Promise<void>
  
  getStreak(): Promise<Streak>
  setStreak(streak: Streak): Promise<void>
  
  getWrongList(): Promise<string[]>
  setWrongList(list: string[]): Promise<void>
  
  incrementWordCorrect(wordId: string): Promise<void>
  incrementWordWrong(wordId: string): Promise<void>
  toggleWordLearned(wordId: string): Promise<void>
}
```

### 4.2 音频接口 (Audio API)

```typescript
interface AudioService {
  speak(text: string, rate?: number): Promise<void>
  stop(): void
  getVoices(): SpeechSynthesisVoice[]
  setRate(rate: number): void
  isSupported(): boolean
  isActivated(): boolean
  activate(): void
}
```

### 4.3 词库管理接口 (WordStore API)

```typescript
interface WordStore {
  getAllWords(group?: string): Word[]
  getWordById(id: string): Word | undefined
  addWord(word: Word): void
  updateWord(id: string, updates: Partial<Word>): void
  deleteWord(id: string): void
  createGroup(name: string): void
  deleteGroup(name: string): void
  importWords(json: string): void
  exportWords(group?: string): string
  searchWords(query: string): Word[]
  getDifficultWords(): Word[]
}
```

### 4.4 数据模型

```typescript
interface Word {
  id: string
  english: string
  chinese: string[]
  group: string
  correct: number
  wrong: number
  lastReview: number
  learned: boolean
}

interface Settings {
  autoPlay: boolean
  rate: number
  showChinese: boolean
  fuzzyMatch: boolean
}

interface Streak {
  lastDate: string
  count: number
  badges: string[]
}

interface QuizResult {
  wordId: string
  isCorrect: boolean
  userAnswer: string
  correctAnswer: string[]
  timestamp: number
}
```

## 5. 数据流向

### 5.1 学习模式数据流程

```
用户进入学习页
    │
    ▼
WordStore.getAllWords(activeGroup)
    │
    ▼
LearningEngine.init(currentIndex=0)
    │
    ├─► AudioMgr.speak(currentWord.english)
    │
    ├─► 用户滑动/点击 → LearningEngine.next()/prev()
    │       │
    │       ▼
    │   更新 currentIndex, currentWord
    │       │
    │       └─► AudioMgr.speak(newWord.english) [autoPlay=true]
    │
    └─► 用户点击"已掌握" → WordStore.toggleWordLearned(wordId)
            │
            ▼
        StorageService.incrementWordCorrect(wordId)
```

### 5.2 默写模式数据流程

```
用户进入默写页
    │
    ▼
QuizMgr.init(words: Word[])
    │
    ▼
显示 currentWord.english
    │
    ▼
用户输入中文 → QuizMgr.validate(input)
    │
    ├─► 匹配成功 → QuizResult(isCorrect=true)
    │       │
    │       ├─► StorageService.incrementWordCorrect(wordId)
    │       │
    │       └─► 显示绿色反馈 → 自动进入下一题
    │
    └─► 匹配失败 → QuizResult(isCorrect=false)
            │
            ├─► StorageService.incrementWordWrong(wordId)
            │
            ├─► StorageService.addToWrongList(wordId)
            │
            └─► 显示红色反馈 + 正确答案 → 自动进入下一题
                    │
                    ▼
            默写结束 → 提示错题复习
```

### 5.3 打卡统计数据流程

```
用户完成学习/默写 (≥5单词)
    │
    ▼
Stats.checkIn()
    │
    ├─► 获取 today = 当前日期
    │
    ├─► 获取 streak.lastDate
    │
    ├─► if streak.lastDate === today → 今日已打卡，跳过
    │
    ├─► if streak.lastDate === yesterday → streak.count++
    │
    └─► else → streak.count = 1
            │
            ▼
        更新 streak.lastDate = today
            │
            ▼
        检查成就条件 → 发放徽章
            │
            ▼
        StorageService.setStreak(streak)
```

## 6. 安全策略

### 6.1 数据安全

| 策略 | 实现 |
|------|------|
| 本地存储加密 | 使用 AES-GCM 加密敏感数据（可选） |
| 存储容量监控 | 写入前检查 localStorage 剩余空间 |
| 异常捕获 | 所有存储操作包裹 try-catch |
| 数据备份 | 导出功能支持完整数据备份 |

### 6.2 XSS防护

| 策略 | 实现 |
|------|------|
| 输入过滤 | 导入单词时过滤 HTML 标签 |
| 输出转义 | Vue 模板自动转义，使用 `v-text` |
| SVG净化 | 图标使用白名单 SVG |

### 6.3 隐私保护

| 策略 | 实现 |
|------|------|
| 零数据上传 | 所有数据仅存储在用户设备 |
| 无痕模式适配 | 检测隐私模式，提供降级提示 |
| 清除数据功能 | 用户可一键清除所有本地数据 |

### 6.4 PWA安全

| 策略 | 实现 |
|------|------|
| HTTPS强制 | manifest 和 SW 注册需 HTTPS |
| 缓存策略 | 网络优先，缓存回退 |
| 更新机制 | SW 检测新版本，提示用户刷新 |

## 7. 目录结构

```
src/
├── components/
│   ├── learning/
│   │   ├── WordCard.vue
│   │   ├── AudioButton.vue
│   │   └── ProgressBar.vue
│   ├── quiz/
│   │   ├── QuizInput.vue
│   │   └── ResultHint.vue
│   ├── wordstore/
│   │   ├── WordList.vue
│   │   ├── GroupManager.vue
│   │   └── ImportExport.vue
│   ├── stats/
│   │   ├── StatsView.vue
│   │   ├── StreakCard.vue
│   │   └── DifficultList.vue
│   └── common/
│       ├── NavBar.vue
│       ├── SettingsPanel.vue
│       └── ShortcutHelp.vue
├── stores/
│   ├── wordStore.ts
│   ├── learningStore.ts
│   ├── quizStore.ts
│   ├── statsStore.ts
│   └── settingsStore.ts
├── services/
│   ├── storage.ts
│   ├── audio.ts
│   ├── gesture.ts
│   └── keyboard.ts
├── data/
│   ├── defaultWords.ts
│   └── achievements.ts
├── utils/
│   ├── uuid.ts
│   ├── date.ts
│   └── matcher.ts
├── router/
│   └── index.ts
├── App.vue
├── main.ts
└── style.css
```

## 8. 构建与部署

### 8.1 构建命令

```bash
npm install
npm run build
```

### 8.2 部署目标

| 平台 | 配置 |
|------|------|
| GitHub Pages | `.github/workflows/deploy.yml` |
| Vercel | 自动检测 Vue + Vite |
| Netlify | 构建命令 `npm run build`，输出目录 `dist` |

### 8.3 PWA配置

- `manifest.json`：定义应用名称、图标、启动方式
- `service-worker.js`：缓存 HTML/CSS/JS/静态资源
- Workbox：自动生成缓存策略

## 9. 性能优化

| 优化项 | 策略 |
|--------|------|
| 首屏加载 | 懒加载路由组件、按需导入 AntD |
| 列表渲染 | 虚拟滚动（长列表）、分页 |
| 语音合成 | 预加载语音列表、缓存语音对象 |
| 存储操作 | 批量写入、防抖处理 |
| CSS优化 | Tailwind 生产环境 Purge |
| Bundle分析 | Vite Bundle Analyzer |

## 10. 兼容性矩阵

| 浏览器 | 版本要求 | 支持特性 |
|--------|----------|----------|
| Chrome | ≥90 | 完整支持 |
| Edge | ≥90 | 完整支持 |
| Firefox | ≥88 | 完整支持 |
| Safari | ≥14 | 语音合成需轮询 |
| iOS Safari | ≥14 | 需用户手势激活音频 |
| Chrome Android | ≥90 | 完整支持 |
| Firefox Android | ≥88 | 完整支持 |
# WordMemo 技术架构文档

> 本文档面向开发者及非技术读者。技术术语旁均附有通俗解释，帮助理解系统设计。

## 1. 系统架构图

系统采用四层结构，从上到下依次为：界面层（用户看到和操作的页面）、业务逻辑层（处理数据和交互规则）、数据存储层（保存数据）、浏览器能力层（调用浏览器自带功能）。

```
┌─────────────────────────────────────────────────────────────────────┐
│                   用户界面层 (UI Layer) — 用户看到的页面                  │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐        │
│  │  学习模块   │  │  默写模块   │  │  词库模块   │  │  统计模块   │        │
│  │  WordCard  │  │  QuizInput │  │  WordList  │  │  StatsView │        │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘        │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────────┐
│              业务逻辑层 (Logic Layer) — 处理数据和交互规则               │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐        │
│  │ Learning   │  │  QuizMgr  │  │ WordStore  │  │   Stats    │        │
│  │  Engine    │  │           │  │            │  │            │        │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘        │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐                       │
│  │  AudioMgr  │  │  Gesture  │  │ KeyHandler │                       │
│  └───────────┘  └───────────┘  └───────────┘                       │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────────┐
│            数据存储层 (Storage Layer) — 把数据保存到浏览器本地            │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    localStorage（浏览器本地存储）                 │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐│   │
│  │  │wordGroups│ │settings │ │ streak  │ │wrongList│ │ wordMap ││   │
│  │  │ (词库)    │ │(设置)    │ │(打卡)    │ │(错题列表) │ │(单词索引)││   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘│   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────────┐
│           浏览器能力层 (Browser Layer) — 浏览器自带功能                  │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐        │
│  │speechSynth│  │  Storage   │  │  Network   │  │  Service   │        │
│  │ (语音合成)  │  │  (存储API)  │  │  (网络API)  │  │  Worker    │        │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘        │
└─────────────────────────────────────────────────────────────────────┘
```

## 2. 模块划分

### 2.1 学习模块 (Learning)

用户浏览单词卡片、听发音、标记掌握状态。

| 子模块 | 职责 | 通俗说明 |
|--------|------|----------|
| WordCard | 单词卡片展示、中英文切换、标记已掌握 | 显示一张大卡片，包含英文单词和中文释义 |
| AudioBtn | 发音按钮交互 | 点击喇叭图标朗读单词 |
| ProgressBar | 学习进度显示 | 顶部或底部显示"第 3/20 个"的进度条 |

### 2.2 默写模块 (Quiz)

看英文写中文，系统自动判分并记录错题。

| 子模块 | 职责 | 通俗说明 |
|--------|------|----------|
| QuizInput | 中文输入框、提交判定 | 用户输入中文答案，系统判断是否正确 |
| ResultHint | 结果反馈动画与提示 | 正确显示绿色，错误显示红色并展示正确答案 |
| WrongCollector | 错题收集与标记 | 自动记录答错的单词，方便后续复习 |

### 2.3 词库模块 (WordStore)

管理所有单词，支持分组、导入导出、搜索。

| 子模块 | 职责 | 通俗说明 |
|--------|------|----------|
| WordList | 单词列表展示、筛选、搜索 | 以表格或卡片形式展示所有单词 |
| GroupMgr | 分组创建、切换、删除 | 按"四级""六级"等分组管理单词 |
| ImportExport | 导入导出 JSON/CSV/PDF/DOCX | 批量导入或导出单词数据 |

### 2.4 统计模块 (Stats)

展示学习成果、打卡记录、成就徽章和难词本。

| 子模块 | 职责 | 通俗说明 |
|--------|------|----------|
| StatsView | 正确率统计、学习数据可视化 | 展示打卡天数、正确率、已学词数 |
| StreakCard | 连续打卡天数展示 | 火焰图标 + 连续天数 + 今日进度 |
| AchievementBadge | 成就徽章展示 | 达成条件时自动获得徽章 |
| DifficultList | 难词本列表 | 错误 3 次以上的单词集中展示 |
| WrongWordList | 错题列表 | 展示所有答错过至少 1 次的单词 |

### 2.5 音频管理模块 (AudioMgr)

调用浏览器语音合成功能朗读单词。

| 功能 | 实现 | 通俗说明 |
|------|------|----------|
| 语音合成 | `window.speechSynthesis` | 使用浏览器自带的朗读功能，无需联网 |
| 语音选择 | 优先 `en-US`，备选 `en-GB` | 优先使用美式发音 |
| 语速调节 | 慢(0.5) / 正常(0.9) / 快(1.5) | 三档速度可调 |
| 自动播放 | 首次激活后自动朗读 | 切换单词时自动发音 |
| 兼容性处理 | iOS Safari 轮询获取语音列表 | 苹果手机需要特殊处理 |

### 2.6 手势处理模块 (GestureHandler)

处理滑动操作，移动端和 PC 端各自适配。

| 手势 | 移动端 | PC端 |
|------|--------|------|
| 左滑 | 上一个单词 | ← 方向键 |
| 右滑 | 下一个单词 | → 方向键 |
| 上滑 | 标记已掌握 | Shift + ↑ |
| 点击卡片 | 发音 | 空格键 |

### 2.7 键盘处理模块 (KeyHandler)

PC 端快捷键支持，提高操作效率。

| 快捷键 | 功能 | 适用场景 |
|--------|------|----------|
| ← / → | 切换单词 | 学习模式 |
| Space | 发音 | 学习模式 |
| Enter | 提交默写答案 | 默写模式 |
| Ctrl+Enter | 提交默写答案 | 默写模式 |
| Tab | 切换焦点 | 默写模式 |
| 1/2/3 | 语速调节 | 全局 |
| ? | 快捷键帮助 | 全局 |

## 3. 技术栈选型

以下列出项目使用的技术及其选择理由。

| 层次 | 技术 | 版本 | 通俗说明 |
|------|------|------|----------|
| 框架 | Vue 3 | 3.4+ | 前端框架，负责构建用户界面 |
| UI组件 | Ant Design Vue | 4.x | 现成的界面组件库，开箱即用 |
| 构建工具 | Vite | 5.x | 将代码打包成浏览器可运行的格式 |
| 状态管理 | Pinia | 2.x | 管理全局数据（如当前单词、设置等） |
| 路由 | Vue Router | 4.x | 实现页面切换，无需刷新浏览器 |
| CSS框架 | Tailwind CSS | 3.x | 用简短的类名直接写样式 |
| 图表 | Chart.js | 4.x | 绘制统计图表 |
| PWA | Workbox | 7.x | 实现离线访问和添加到手机主屏幕 |
| 图标 | Lucide Vue | latest | 轻量 SVG 图标库 |

## 4. 接口设计

### 4.1 存储接口 (Storage API)

负责所有数据的读写操作，底层使用浏览器 `localStorage`（可理解为浏览器提供的一个小型本地数据库，一次写入约 10MB）。

```typescript
interface StorageService {
  getWordGroups(): Promise<Record<string, Word[]>>
  setWordGroups(groups: Record<string, Word[]>): Promise<void>

  getActiveGroup(): Promise<string>
  setActiveGroup(group: string): Promise<void>

  getSettings(): Promise<Settings>
  setSettings(settings: Settings): Promise<void>

  getStreak(): Promise<Streak>
  setStreak(streak: Streak): Promise<void>

  getWrongList(): Promise<string[]>
  setWrongList(list: string[]): Promise<void>

  incrementWordCorrect(wordId: string): Promise<void>
  incrementWordWrong(wordId: string): Promise<void>
  toggleWordLearned(wordId: string): Promise<void>
}
```

### 4.2 音频接口 (Audio API)

```typescript
interface AudioService {
  speak(text: string, rate?: number): Promise<void>
  stop(): void
  getVoices(): SpeechSynthesisVoice[]
  setRate(rate: number): void
  isSupported(): boolean      // 浏览器是否支持语音合成
  isActivated(): boolean       // 是否已通过用户点击激活
  activate(): void             // 激活（需用户手势触发）
}
```

### 4.3 词库管理接口 (WordStore API)

```typescript
interface WordStore {
  getAllWords(group?: string): Word[]
  getWordById(id: string): Word | undefined
  addWord(word: Word): void
  updateWord(id: string, updates: Partial<Word>): void
  deleteWord(id: string): void
  createGroup(name: string): void
  deleteGroup(name: string): void
  importWords(json: string): void
  exportWords(group?: string): string
  searchWords(query: string): Word[]
  getDifficultWords(): Word[]   // 获取错误 >= 3 次的难词
}
```

### 4.4 数据模型

系统中核心的数据结构定义。

```typescript
// 单词对象
interface Word {
  id: string          // 唯一标识，自动生成
  english: string     // 英文单词
  chinese: string[]   // 中文释义（支持多个，如 ["苹果","苹果公司"]）
  group: string       // 所属分组
  correct: number     // 默写正确次数
  wrong: number       // 默写错误次数
  lastReview: number  // 最后复习时间戳
  learned: boolean    // 是否标记为已掌握
}

// 用户设置
interface Settings {
  autoPlay: boolean   // 切换单词后是否自动发音
  rate: number        // 语速：0.5/0.9/1.5
  showChinese: boolean // 是否默认显示中文释义
  fuzzyMatch: boolean  // 是否开启模糊匹配（忽略标点）
}

// 打卡记录
interface Streak {
  lastDate: string    // 上次打卡日期
  count: number       // 连续打卡天数
  badges: string[]    // 已获得的成就徽章 ID 列表
}

// 默写答题结果
interface QuizResult {
  wordId: string
  isCorrect: boolean
  userAnswer: string
  correctAnswer: string[]
  timestamp: number
}
```

**localStorage 存储结构**（浏览器本地存储中的数据格式）：

```json
{
  "wordGroups": { "四级基础": [...], "六级进阶": [...] },
  "activeGroup": "四级基础",
  "settings": { "autoPlay": true, "rate": 0.9, "showChinese": false, "fuzzyMatch": false },
  "streak": { "lastDate": "2026-07-25", "count": 1, "badges": ["first_day"] },
  "wrongList": ["id1", "id2"]
}
```

## 5. 数据流向

### 5.1 学习模式数据流程

```
用户进入学习页
    │
    ▼
加载当前分组的全部单词
    │
    ▼
从第 1 个单词开始，自动朗读（如已开启自动播放）
    │
    ├─► 用户滑动/点击"下一个" → 切换到下一个单词，自动朗读
    │
    ├─► 用户点击"标记已掌握" → 保存到存储 → 刷新统计数据
    │       │
    │       └─► 单词状态变为"已掌握"，按钮文字变为"取消已掌握"
    │
    └─► 用户点击"取消已掌握" → 还原为未掌握状态，统计数据同步更新
```

### 5.2 默写模式数据流程

```
用户进入默写页
    │
    ▼
显示英文单词，等待用户输入中文
    │
    ▼
用户提交答案 → 系统判定
    │
    ├─► 正确 → 正确次数 +1 → 绿色反馈 → 自动进入下一题
    │
    └─► 错误 → 错误次数 +1 → 加入错题列表 → 红色反馈 + 显示正确答案
            │
            └─► 若错误次数 ≥ 3 → 自动标记为"难词"
                    │
                    ▼
            默写全部结束 → 弹窗统计错题数 → 可选择"立即复习错题"
```

### 5.3 打卡统计数据流程

```
用户完成学习/默写（≥ 5 个单词）
    │
    ▼
检查打卡条件
    │
    ├─► 今天已经打过卡 → 跳过
    │
    ├─► 昨天打过卡 → 连续天数 +1
    │
    └─► 间隔超过 1 天 → 连续天数重置为 1
            │
            ▼
        更新打卡日期和天数
            │
            ▼
        检查成就条件 → 满足则自动发放徽章
            │
            ▼
        保存到本地存储
```

### 5.4 错题复习流程

```
默写完成 → 统计错误数量
    │
    ├─► 错误数为 0 → 显示完成弹窗
    │
    └─► 错误数 > 0 → 弹出复习弹窗
            │
            ├─► "立即复习" → 仅加载错题进入默写模式
            │
            └─► "稍后复习" → 返回学习页，错题保留在列表中
```

## 6. 安全策略

### 6.1 数据安全

| 策略 | 实现 | 通俗说明 |
|------|------|----------|
| 存储容量监控 | 写入前检查剩余空间 | 防止存储满了导致数据丢失 |
| 异常捕获 | 所有存储操作包裹 try-catch | 存储出错时不会崩溃，给出提示 |
| 数据备份 | 导出功能支持完整数据备份 | 用户可导出 JSON 文件备份所有数据 |

### 6.2 XSS 防护（防止恶意代码注入）

| 策略 | 实现 | 通俗说明 |
|------|------|----------|
| 输入过滤 | 导入单词时过滤 HTML 标签 | 防止用户导入含恶意代码的数据 |
| 输出转义 | Vue 模板自动转义 | 框架自动处理，开发者无需额外操作 |
| SVG 净化 | 图标使用白名单 SVG | 只加载安全的图标文件 |

### 6.3 隐私保护

| 策略 | 实现 | 通俗说明 |
|------|------|----------|
| 零数据上传 | 所有数据仅存储在用户设备 | 用户的单词数据不会发送到任何服务器 |
| 无痕模式适配 | 检测隐私模式，提供降级提示 | 浏览器隐私模式下可能无法存储 |
| 清除数据 | 用户可一键清除所有本地数据 | 设置中提供"清除所有数据"按钮 |

### 6.4 PWA 安全

| 策略 | 实现 | 通俗说明 |
|------|------|----------|
| HTTPS 强制 | 部署时必须使用 HTTPS | 加密传输，保护数据安全 |
| 缓存策略 | 网络优先，缓存回退 | 有网时用最新版本，没网时用缓存版本 |
| 更新机制 | 检测新版本，提示用户刷新 | 应用更新后自动提示 |

## 7. 目录结构

```
src/
├── components/
│   ├── learning/          # 学习模块组件
│   │   ├── WordCard.vue       # 单词卡片
│   │   ├── AudioButton.vue    # 发音按钮
│   │   └── ProgressBar.vue    # 进度条
│   ├── quiz/              # 默写模块组件
│   │   ├── QuizInput.vue      # 默写输入框
│   │   └── ResultHint.vue     # 结果反馈
│   ├── wordstore/         # 词库模块组件
│   │   ├── WordList.vue       # 单词列表
│   │   ├── GroupManager.vue   # 分组管理
│   │   └── ImportExport.vue   # 导入导出
│   ├── stats/             # 统计模块组件
│   │   ├── StatsView.vue      # 统计面板
│   │   ├── StreakCard.vue     # 打卡卡片
│   │   ├── AchievementBadge.vue # 成就徽章
│   │   ├── DifficultList.vue  # 难词本
│   │   └── WrongWordList.vue  # 错题列表
│   └── common/            # 公共组件
│       ├── NavBar.vue         # 导航栏
│       ├── SettingsPanel.vue  # 设置面板
│       └── ShortcutHelp.vue   # 快捷键帮助
├── stores/                # 状态管理（数据存储和逻辑）
│   ├── wordStore.ts           # 词库状态
│   ├── learningStore.ts       # 学习状态
│   ├── quizStore.ts           # 默写状态
│   ├── statsStore.ts          # 统计状态
│   └── settingsStore.ts       # 设置状态
├── services/              # 服务层（封装浏览器能力）
│   ├── storage.ts             # 存储服务
│   ├── audio.ts               # 音频服务
│   ├── gesture.ts             # 手势服务
│   └── keyboard.ts            # 键盘服务
├── data/                  # 静态数据
│   ├── defaultWords.ts        # 默认词库
│   └── achievements.ts        # 成就徽章定义
├── utils/                 # 工具函数
│   ├── uuid.ts                # 生成唯一 ID
│   ├── date.ts                # 日期处理
│   └── matcher.ts             # 答案匹配
├── router/
│   └── index.ts               # 路由配置
├── App.vue                    # 根组件
├── main.ts                    # 入口文件
└── style.css                  # 全局样式
```

## 8. 构建与部署

### 8.1 构建命令

```bash
npm install          # 安装依赖
npm run build        # 构建生产版本，输出到 dist/ 目录
npm run dev          # 启动开发服务器，支持热更新
```

### 8.2 部署目标

项目构建产物为纯静态文件（HTML + CSS + JS），无需服务器，可部署到任意静态托管平台。

| 平台 | 说明 |
|------|------|
| GitHub Pages | 免费托管，需配置 `.github/workflows/deploy.yml` |
| Vercel | 自动检测 Vue + Vite 项目，一键部署 |
| Netlify | 构建命令 `npm run build`，输出目录 `dist` |

### 8.3 PWA 配置

应用支持 PWA（渐进式 Web 应用），可像原生 App 一样添加到手机主屏幕，支持离线使用。

- `manifest.json`：定义应用名称、图标、启动方式
- `service-worker.js`：缓存所有资源文件，离线时从缓存加载
- Workbox：自动生成缓存策略，无需手动编写 Service Worker

## 9. 性能优化

| 优化项 | 策略 | 通俗说明 |
|--------|------|----------|
| 首屏加载 | 路由组件懒加载、按需导入 UI 组件 | 首次打开只加载当前页面，加快启动速度 |
| 列表渲染 | 超长列表使用虚拟滚动 | 只渲染屏幕可见区域，节省性能 |
| 语音合成 | 预加载语音列表、缓存语音对象 | 减少首次发音的等待时间 |
| 存储操作 | 批量写入、防抖处理 | 避免频繁写入导致的卡顿 |
| CSS 优化 | 生产环境自动移除未使用的样式 | 减小最终文件体积 |
| 打包分析 | Vite Bundle Analyzer | 可视化分析各模块大小，针对性优化 |

## 10. 兼容性矩阵

| 浏览器 | 最低版本 | 注意事项 |
|--------|----------|----------|
| Chrome | 90 | 完整支持 |
| Edge | 90 | 完整支持 |
| Firefox | 88 | 完整支持 |
| Safari | 14 | 语音合成需额外处理 |
| iOS Safari | 14 | 首次需要用户点击激活音频 |
| Chrome Android | 90 | 完整支持 |
| Firefox Android | 88 | 完整支持 |
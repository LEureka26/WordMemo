# 项目名称：WordMemo（双端自适应单词学习工具）

### 1. 项目概述

一个**纯静态、零后端**的英语单词学习 PWA，**深度适配 PC 与移动端**。核心功能为单词发音和中文默写。通过响应式布局与交互差异化设计，让用户在电脑、手机、平板上均获得高效、流畅的学习体验。部署后即可通过浏览器访问，支持离线学习。

### 2. 双端用户画像与场景

- **移动端用户**：利用通勤、排队等碎片时间，单手操作。需求：快速切换单词、一键发音、便捷默写。期望卡片式界面简洁，按钮大且易触控。
- **PC 端用户**：桌面沉浸式学习，可能长时间使用。需求：键盘快捷键高效操作，宽屏下多信息并行展示（如单词列表+卡片），能批量管理词库。

### 3. 核心功能需求（带双端区分）

#### 3.1 单词学习与发音模式

| 功能点      | 移动端                                                                   | PC 端                                                   |
| -------- | --------------------------------------------------------------------- | ------------------------------------------------------ |
| **基础交互** | 卡片占满主要区域，左右滑动切换单词，点击卡片发音。底部固定大按钮：“学过了/下一个”。                           | 左侧单词列表（可收缩），右侧大卡片。点击列表项切换，支持键盘 ← → 方向键切换。空格键或点击喇叭图标发音。 |
| **自动发音** | 切换单词后自动朗读，但需遵循浏览器**用户手势限制**：初次使用时需引导用户点击“开始学习”按钮以激活音频上下文。之后自动播放可正常工作。 | 同样需首次激活，后续自动播放不受限制。提供更明显的自动播放开关。                       |
| **语速调节** | 底部工具栏提供“慢/正常/快”切换按钮。                                                  | 顶部设置栏或快捷键（如 `1,2,3`）调节语速。                              |
| **中文提示** | 默认隐藏中文，点击“显示释义”浮层展示，再次点击隐藏。                                           | 卡片下方默认展示较小中文，可设置常显/常隐。                                 |

#### 3.2 默写模式（看英文写中文）

- **核心流程**：展示英文单词 → 输入中文 → 提交判定 → 反馈结果 → 自动下一题。
- **输入方式适配**：
  - **移动端**：输入框获取焦点后，手机键盘弹起，输入框保持在可视区域。提供“确定”按钮，也支持键盘回车提交。输入法候选词不影响判定。
  - **PC 端**：输入框自动聚焦，回车键提交。可使用 `Tab` 键快速切换焦点，支持 `Ctrl+Enter` 也可以提交。
- **答案匹配逻辑**：
  - 基础：去除首尾空格后完全匹配。
  - 多义支持：单词可配置 `["苹果","苹果公司"]`，用户输入任一即判对。
  - 可选**模糊匹配**：忽略全角/半角标点（如输入“苹果，”也正确），可在设置中开启。
- **结果反馈**：
  - 正确：绿色闪烁 + 直接在卡片下方用卡片的形式显示出正确详细解释。
  - 错误：红色抖动 + 显示正确答案并附带详细解释，同时单词加入错题集。
  - **移动端**：结果覆盖卡片区域，2秒后自动消失进入下一题。
  - **PC 端**：结果在卡片下方以标签形式展示，显示“√ 正确”或“× 正确答案：xxx”，2秒后自动消失。同时左侧列表对应单词高亮红/绿色标记。
- **进度与统计**：顶部进度条（如“第 3/20 题”）。移动端在顶部，PC 端在侧边栏显示详细正确率饼图或进度条。

#### 3.3 词库管理

##### 3.3.1 功能概述

词库管理模块是用户维护学习内容的入口，提供分组组织、批量导入导出、单词增删改查等能力。所有数据本地持久化，零服务端依赖。

| 功能点     | 优先级 | 描述                        |
| ------- | --- | ------------------------- |
| 默认词库初始化 | P0  | 首次加载自动写入内置四级词库            |
| 分组管理    | P0  | 创建、删除、切换分组                |
| 单词 CRUD | P0  | 添加、编辑、删除单词（含中英文释义）        |
| 批量导入    | P0  | 支持 JSON/CSV/DOCX/PDF 文件导入 |
| 数据导出    | P0  | 按分组导出 JSON 文件             |
| 列表搜索筛选  | P1  | 关键词搜索、按分组筛选               |
| 内联编辑    | P1  | 双击单词直接编辑，回车保存             |
| 重复检测    | P1  | 导入时自动跳过已存在的单词             |
| 容量监控    | P2  | 接近 localStorage 上限时提示用户   |

##### 3.3.2 用户角色

| 角色   | 权限               |
| ---- | ---------------- |
| 普通用户 | 全部操作（无服务端，无权限分级） |

##### 3.3.3 业务流程

**3.3.3.1 导入单词流程**

```
用户触发导入
    │
    ├─ 粘贴文本 → 解析 JSON 格式文本 → 校验字段 → 合并到当前分组
    │
    └─ 选择文件 → 判断文件类型
            ├─ .json → JSON.parse → 校验字段 → 合并到当前分组
            ├─ .csv  → 按行解析 → 提取 english/chinese → 生成 Word 对象
            ├─ .docx → 读取段落文本 → 提取英文/中文对 → 生成 Word 对象
            └─ .pdf  → 提取文本内容 → 按行解析 → 生成 Word 对象
                    │
                    ▼
              重复检测 → 跳过已有单词 → 写入 localStorage
                    │
                    ▼
              显示导入结果（成功 N 条，跳过 M 条，失败 K 条）
```

**3.3.3.2 导出单词流程**

```
用户点击导出 → 选择分组 → 序列化为 JSON → 触发浏览器下载
```

**3.3.3.3 分组管理流程**

```
创建分组 → 输入名称 → 写入 wordGroups → 刷新列表
删除分组 → 确认弹窗 → 删除分组数据 → 若为当前分组则切换到默认分组
```

##### 3.3.4 数据字典

**3.3.4.1 Word 对象**

| 字段         | 类型        | 必填 | 说明               |
| ---------- | --------- | -- | ---------------- |
| id         | string    | 是  | UUID，导入时自动生成     |
| english    | string    | 是  | 英文单词，唯一标识（同分组内）  |
| chinese    | string\[] | 是  | 中文释义数组，支持多义      |
| group      | string    | 是  | 所属分组名称           |
| correct    | number    | 否  | 默写正确次数，默认 0      |
| wrong      | number    | 否  | 默写错误次数，默认 0      |
| lastReview | number    | 否  | 最后复习时间戳，默认 0     |
| learned    | boolean   | 否  | 是否标记已掌握，默认 false |

**3.3.4.2 导入格式规范**

**JSON 格式**：

```json
[
  {
    "english": "abandon",
    "chinese": ["放弃", "遗弃"],
    "group": "四级基础"
  }
]
```

**CSV 格式**（逗号分隔，多义用分号分隔）：

```
english,chinese
abandon,放弃;遗弃
ability,能力;才能
```

**DOCX/PDF 格式**：按行解析，每行格式为 `english 中文释义` 或 `english: 中文释义`。

##### 3.3.5 界面设计

**3.3.5.1 移动端**

- 底部 Tab 进入词库页
- 顶部：分组标签栏（横向滚动），右侧"+"新建分组
- 搜索框：实时过滤单词列表
- 单词列表：卡片式，左侧英文，右侧中文简写，底部显示掌握状态色点
- 底部固定栏："导入"按钮、"添加单词"按钮
- 导入面板：底部弹出，提供"粘贴文本"和"选择文件"两个入口

**3.3.5.2 PC 端**

- 左侧词库面板（可折叠）
- 顶部：分组下拉选择器 + 搜索框
- 单词列表：表格式展示，支持表头排序
- 双击行进入内联编辑，回车保存，Esc 取消
- 工具栏：导入按钮、导出按钮、添加单词按钮
- 导入支持拖拽文件到指定区域

##### 3.3.6 非功能性需求

| 类别  | 要求              | 指标                            |
| --- | --------------- | ----------------------------- |
| 性能  | 词库列表渲染          | 1000 词以内 < 200ms              |
| 性能  | 导入解析            | 500 词 < 1s                    |
| 容量  | localStorage 上限 | 10MB，约可存储 50000+ 词            |
| 安全  | 输入过滤            | 英文仅保留字母，中文过滤 HTML 标签          |
| 安全  | 文件校验            | 限制文件大小 ≤ 5MB                  |
| 易用性 | 导入反馈            | 显示成功/跳过/失败数量                  |
| 易用性 | 错误提示            | 格式错误、容量不足等场景给出明确提示            |
| 兼容性 | 文件格式            | JSON/CSV 全端支持，DOCX/PDF 需引入解析库 |

##### 3.3.7 异常处理

| 场景              | 处理方式                      |
| --------------- | ------------------------- |
| localStorage 已满 | 提示用户清理数据或导出备份             |
| 导入格式错误          | 跳过错误行，汇总后提示用户             |
| 重复单词            | 自动跳过，提示"已存在，已跳过"          |
| 文件过大（>5MB）      | 拒绝导入，提示用户拆分文件             |
| 隐私模式不可用         | 检测并提示用户切换正常模式             |
| DOCX/PDF 解析失败   | 提示用户该文件无法解析，建议使用 JSON/CSV |

##### 3.3.8 验收标准

- [ ] 首次打开应用自动加载默认四级词库
- [ ] 可创建/删除分组，删除时有确认弹窗
- [ ] 可添加单词（英文 + 至少一个中文释义）
- [ ] 可导入 JSON 文件并正确合并到当前分组
- [ ] 可导入 CSV 文件并正确解析
- [ ] 重复单词导入时自动跳过
- [ ] 可导出当前分组为 JSON 文件
- [ ] 搜索框实时过滤单词列表
- [ ] 默认分组不可删除
- [ ] 刷新页面后词库数据不丢失

#### 3.4 错题复习与难词本

##### 3.4.1 功能概述

错题复习模块对默写中产生的错误进行追踪与管理，帮助用户针对性复习薄弱单词。难词本自动标记高频错误单词，统计页提供集中查看入口。核心数据流：quizStore.validate() 判定答案 → 错误时 incrementWordWrong() 更新存储 → wrongList 持久化到 localStorage → 难词判定（wrong >= 3）→ 统计页展示。

| 功能点 | 优先级 | 描述 | 实现要点 |
|--------|--------|------|----------|
| 错误记录 | P0 | 默写错误时自动记录单词 ID，累加错误次数，持久化到 localStorage | quizStore.validate() 中调用 incrementWordWrong()，同时更新 Word.wrong 字段 |
| 错题复习弹窗 | P0 | 默写结束后提示错误数量，支持"立即复习"和"稍后复习"两个操作入口 | quizStore.isFinished 触发弹窗，wrongCount 驱动显示 |
| 错题重做 | P0 | 仅使用错题列表重新进入默写模式，筛选 wrong > 0 的单词 | quizStore.init(group, useWrongList=true) 过滤单词列表 |
| 难词自动标记 | P1 | 错误次数 >= 3 的单词自动归类为"难词"，无需手动操作 | 计算属性过滤 wrong >= 3，实时响应数据变化 |
| 难词列表展示 | P1 | 统计页展示难词，每项显示英文、中文释义、错误/正确次数，点击跳转学习页并定位 | DifficultList.vue 组件渲染，emit('select') 触发路由跳转 |
| 难词筛选学习 | P2 | 学习模式中可按难词进行筛选，仅展示难词列表中的单词 | 学习页添加筛选下拉，切换时重新加载对应单词列表 |
| 错题历史追溯 | P2 | 记录每次答题的时间戳和结果，支持查看历史错题详情 | QuizResult 含 timestamp 字段，可扩展历史查询 |
| 错题导出 | P3 | 支持将错题列表导出为 JSON/CSV 格式，便于离线复习 | 复用词库管理模块的导出逻辑 |

##### 3.4.2 业务流程

**3.4.2.1 错题记录流程**

```
用户提交答案 -> 判定错误
    |
    +--> 记录 QuizResult -> 存入 results 数组
    +--> 累加 Word.wrong 次数
    +--> 将 wordId 加入 wrongList（存储层）
    +--> 若 wrongCount >= 3 -> 自动标记为难词
```

**3.4.2.2 错题复习流程**

```
默写完成 -> 统计 wrongCount
    |
    +--> wrongCount === 0 -> 显示完成弹窗
    |
    +--> wrongCount > 0 -> 弹出错题复习弹窗
            |
            +--> 用户选择"立即复习" -> quizStore.init(activeGroup, retryWrong=true)
            |       +--> 仅加载 wrongList 中的单词 -> 重新进入默写
            |
            +--> 用户选择"稍后复习" -> 返回学习页
                    +--> 错误单词保留在 wrongList 中，下次默写时优先出现
```

**3.4.2.3 难词管理流程**

```
统计页查看难词列表
    |
    +--> 展示所有 wrongCount >= 3 的单词
    +--> 每项显示：英文、中文释义、错误次数、正确次数
    +--> 点击单词 -> 跳转学习页 -> 定位到该单词
```

##### 3.4.3 数据字典

**3.4.3.1 QuizResult 对象**

| 字段 | 类型 | 描述 |
|------|------|------|
| wordId | string | 单词 ID |
| isCorrect | boolean | 是否正确 |
| userAnswer | string | 用户输入答案 |
| correctAnswer | string[] | 正确答案列表 |
| timestamp | number | 答题时间戳 |

**3.4.3.2 难词判定规则**

| 条件 | 分类 |
|------|------|
| wrong >= 3 | 难词 |
| wrong >= 1 | 学习中 |
| wrong === 0 | 未出错 |

##### 3.4.4 界面设计

**移动端**：
- 默写完成弹窗居中显示，错误数量红色高亮
- 两个按钮："稍后复习"（outline）、"立即复习"（primary）

**PC 端**：
- 统计页左侧面板展示难词列表
- 每行显示英文、中文、错误次数标签
- 点击行跳转学习页并定位到对应单词

##### 3.4.5 非功能性需求

| 类别 | 要求 | 指标 |
|------|------|------|
| 性能 | 错题列表加载 | 100 词以内 < 50ms |
| 数据 | 错题持久化 | 刷新页面后 wrongList 不丢失 |
| 易用性 | 复习提示 | 明确显示错误数量和操作入口 |

##### 3.4.6 异常处理

| 场景 | 处理方式 |
|------|----------|
| 错题列表为空时点击复习 | 提示"暂无错题，无需复习" |
| 所有错题被删除 | 复习时自动跳过已删除单词 |
| 默写0题 | 不触发打卡和错题统计 |

##### 3.4.7 验收标准

- [ ] 默写错误后单词正确加入 wrongList
- [ ] 默写结束后弹出错误统计和复习弹窗
- [ ] 点击"立即复习"仅加载错题进入默写
- [ ] 错误次数 >= 3 的单词自动标记为难词
- [ ] 统计页难词列表正确显示
- [ ] 点击难词可跳转学习页并定位

##### 3.4.8 性能优化方案

| 优化项 | 当前方案 | 优化方案 | 预期收益 |
|--------|----------|----------|----------|
| 错题列表加载 | 全量遍历 allWords 过滤 wrong > 0 | 维护独立的 wrongWordIds Set，O(1) 查找 | 1000 词场景下加载耗时从 ~20ms 降至 ~1ms |
| 难词判定 | 每次渲染时计算 wrong >= 3 | 使用 computed 缓存，仅在 wrong 值变化时重算 | 减少不必要的重复计算 |
| localStorage 读写 | 每次答题后同步写入 | 批量写入：答题结束后统一持久化，中间状态仅存内存 | 减少 I/O 次数，50 题默写从 50 次写入降至 1 次 |
| 单词列表渲染 | 全量渲染 | 虚拟滚动（超 100 词时启用） | 大词库场景下渲染性能提升 10x+ |
| 错题复习初始化 | 每次重新遍历过滤 | 缓存错题 ID 列表，仅在答题结果变化时更新 | 复习入口响应速度提升 |

##### 3.4.9 用户体验改进措施

| 改进项 | 描述 | 实现建议 |
|--------|------|----------|
| 渐进式复习提示 | 错题数 1-3 个：轻提示；4-10 个：中等提示；>10 个：强调提示 | 根据 wrongCount 动态调整弹窗样式和文案 |
| 错题掌握度可视化 | 错题列表显示错误次数，用颜色深浅表示掌握程度 | 错误 1 次浅红，2-3 次中红，>=3 次深红 |
| 一键清空错题 | 提供"全部已掌握"按钮，一键清空错题列表 | 确认弹窗后调用 setWrongList([]) 并重置所有 Word.wrong |
| 错题复习进度标识 | 错题复习时显示"第 X/Y 题（错题复习）"标识 | quizStore 添加 isRetryMode 状态，QuizView 显示对应标签 |
| 难词突破挑战 | 难词数 >= 10 时，提示"难词突破挑战"入口 | 统计页检测难词数量，动态显示挑战入口卡片 |
| 复习提醒通知 | 错题 > 0 且超 24 小时未复习，首页显示提醒 | 使用页面内提醒组件，首页顶部展示 |

##### 3.4.10 技术难点与解决方案

| 难点 | 描述 | 解决方案 |
|------|------|----------|
| 错题数据一致性 | Word.wrong 字段与 wrongList 数组可能不同步（如单词被删除） | 以 wrongList 为唯一数据源，Word.wrong 仅作展示用；单词删除时同步清理 wrongList |
| 大词库性能 | 词库超 5000 词时，全量遍历过滤性能下降 | 使用 Map<wordId, Word> 索引结构替代数组遍历；错题列表使用 Set 存储 ID |
| 跨设备同步 | 当前仅 localStorage 存储，无法跨设备 | 预留云同步接口（P2），数据结构设计兼容云端同步字段 |
| 默写中断恢复 | 用户中途退出默写，错题数据可能丢失 | 每次答题后立即写入 localStorage；页面卸载前调用 save() |
| 难词判定时机 | 错误次数刚好到 3 时需立即标记 | 在 incrementWordWrong() 内部检查 wrong >= 3 并触发难词标记事件 |

---

#### 3.5 学习激励与打卡

##### 3.5.1 功能概述

打卡与成就系统通过连续天数追踪和徽章奖励机制，激励用户保持每日学习习惯。核心数据流：statsStore.checkIn() 判定打卡条件 → 更新 streak.count 和 lastDate → checkAchievements() 遍历徽章条件 → setStreak() 持久化到 localStorage。

| 功能点 | 优先级 | 描述 | 实现要点 |
|--------|--------|------|----------|
| 连续打卡 | P0 | 每日完成 >= 5 个单词的学习或默写计为打卡 | statsStore.checkIn(wordsCount) 中判定，wordsCount >= 5 触发 |
| 打卡天数展示 | P0 | 移动端首页卡片、PC 端侧边栏显示连续天数 | StreakCard.vue 组件读取 streak.count 渲染 |
| 成就徽章 | P1 | 达成条件时自动发放徽章，支持多维度条件判定 | achievements 数组配置 condition 函数，checkAchievements() 遍历检查 |
| 徽章展示 | P1 | 统计页展示已获得徽章列表，含徽章名称和图标 | statsStore.getBadgeName() 解析 ID，统计页网格布局渲染 |
| 打卡动画反馈 | P2 | 打卡成功时火焰图标播放缩放动画，连续里程碑弹出祝贺 | CSS @keyframes 动画 + streak.count 里程碑检测 |
| 学习目标设定 | P2 | 用户可自定义每日学习目标词数（默认 20 词） | 设置页添加目标词数配置，statsStore 读取并计算进度 |

##### 3.5.2 打卡规则

| 规则 | 说明 |
|------|------|
| 触发条件 | 单次学习或默写 >= 5 个单词 |
| 同日重复 | 同一天多次完成不计为多次打卡 |
| 连续判定 | 昨天打卡 -> 今天 +1；间隔 > 1 天 -> 重置为 1 |
| 存储字段 | streak.lastDate（日期字符串）、streak.count（连续天数） |

##### 3.5.3 成就徽章定义

| 徽章 ID | 名称 | 触发条件 | 优先级 |
|----------|------|----------|--------|
| first_day | 首日打卡 | streak >= 1 | P0 |
| seven_days | 连续7天 | streak >= 7 | P1 |
| perfect_quiz | 默写全对 | 单次默写 >= 5 题且全部正确 | P1 |
| hundred_words | 百词达人 | 累计学习 >= 100 个不同单词 | P1 |
| perfect_week | 完美一周 | streak >= 7 | P2 |

##### 3.5.4 业务流程

**3.5.4.1 打卡流程**

```
用户完成学习/默写（wordsCount >= 5）
    |
    +--> 获取 today = 当前日期
    +--> 获取 streak.lastDate
    |
    +--> lastDate === today -> 今日已打卡，跳过
    |
    +--> lastDate === yesterday -> streak.count++
    |
    +--> 其他情况 -> streak.count = 1
            |
            v
        streak.lastDate = today
            |
            v
        checkAchievements() -> 检查徽章条件
            |
            v
        setStreak(streak) -> 持久化到 localStorage
```

**3.5.4.2 成就检查流程**

```
checkAchievements()
    |
    +--> 遍历 achievements 列表
    +--> 跳过已获得的徽章（streak.badges 中已存在）
    +--> 调用 achievement.condition(streak, correct, total, learned)
    +--> 条件满足 -> 推入 streak.badges
```

##### 3.5.5 界面设计

**移动端**：
- 首页顶部 StreakCard：大号火焰图标 + 连续天数 + 进度环
- 统计页"我的"Tab：徽章墙展示已获得徽章

**PC 端**：
- 侧边栏 StreakCard：紧凑型卡片，显示天数 + 目标进度
- 统计页：徽章网格 + 未获得徽章灰色显示

##### 3.5.6 非功能性需求

| 类别 | 要求 | 指标 |
|------|------|------|
| 数据 | 打卡持久化 | 刷新页面后 streak 不丢失 |
| 数据 | 时区处理 | 使用本地日期，避免跨时区问题 |
| 易用性 | 视觉反馈 | 打卡成功时 StreakCard 有短暂动画 |

##### 3.5.7 异常处理

| 场景 | 处理方式 |
|------|----------|
| 首次使用无 streak 数据 | 初始化为 { lastDate: '', count: 0, badges: [] } |
| localStorage 数据损坏 | 读取时 try-catch，失败则使用默认值 |
| 跨天边界 | 使用 getToday()/getYesterday() 工具函数统一处理 |

##### 3.5.8 验收标准

- [ ] 每日首次完成 >= 5 词学习后打卡天数 +1
- [ ] 同一天多次完成不重复计算
- [ ] 间隔超过 1 天未打卡，连续天数重置为 1
- [ ] 达成条件时自动获得徽章
- [ ] 统计页正确展示已获得徽章
- [ ] 刷新页面后打卡数据和徽章不丢失

##### 3.5.9 性能优化方案

| 优化项 | 当前方案 | 优化方案 | 预期收益 |
|--------|----------|----------|----------|
| 成就检查 | 每次打卡后遍历全部成就 | 使用 Map 缓存已检查成就，仅检查未获得项 | 成就数 > 20 时检查耗时从 O(n) 趋近 O(1) |
| 打卡日期计算 | 每次调用 getToday()/getYesterday() | 在 store 初始化时缓存当天日期，避免重复计算 | 减少 Date 对象创建 |
| 徽章渲染 | 全量渲染所有徽章（含未获得） | 已获得/未获得分组渲染，未获得使用 CSS 灰度滤镜 | 视觉反馈更清晰，无额外性能开销 |

##### 3.5.10 用户体验改进措施

| 改进项 | 描述 | 实现建议 |
|--------|------|----------|
| 打卡动画优化 | 打卡成功时火焰图标播放缩放 + 粒子动画 | CSS @keyframes + requestAnimationFrame 控制动画帧 |
| 里程碑提示 | 连续 3/7/30/100 天时弹出特殊祝贺弹窗 | 检查 streak.count 是否为里程碑值，触发对应弹窗 |
| 今日进度环 | 首页显示今日学习进度环（已学/目标） | SVG 环形进度条，目标默认 20 词/天，可在设置中调整 |
| 成就分享 | 获得成就时支持生成分享卡片 | 使用 Canvas 渲染分享图，支持保存到相册 |
| 补签机制 | 连续打卡中断 1 天时，可通过完成额外任务补签 | 完成 2 倍单词量（>= 10 词）可恢复连续天数 |
| 弱网环境提示 | 离线时打卡数据暂存，恢复网络后同步 | Service Worker 缓存 + Background Sync API |

##### 3.5.11 技术难点与解决方案

| 难点 | 描述 | 解决方案 |
|------|------|----------|
| 时区跨天判定 | 用户跨时区使用时，打卡日期可能错乱 | 统一使用本地时区 getToday()，不依赖服务器时间；工具函数封装时区处理逻辑 |
| 成就条件复杂度 | 部分成就条件涉及多维度数据（streak + correct + total） | 使用策略模式封装每个成就的 condition 函数，便于扩展和维护 |
| localStorage 容量限制 | 长时间使用后数据量增大，可能超出 5MB 限制 | 定期清理 30 天前的答题记录；压缩存储 JSON 数据 |
| 打卡数据损坏恢复 | localStorage 数据被意外修改或损坏 | 读取时 try-catch 包裹，失败回退默认值；提供数据导出备份功能 |
| 多标签页同步 | 多个标签页同时打卡可能导致数据冲突 | 使用 BroadcastChannel API 或 localStorage 事件监听，同步 streak 状态 |
| 成就通知去重 | 同一成就多次触发时不应重复通知 | 检查 badges 数组是否已包含该成就 ID，已存在则跳过通知 |

### 4. 双端界面布局设计

#### 移动端布局（宽度 < 768px）

- **底部 Tab 导航**：学习 / 默写 / 词库 / 我的（统计）
- **学习页**：全屏单词卡片垂直居中，顶部小字“第 1/50 个”，下方一排圆形指示点。底部工具栏：喇叭、显示中文、自动播放、语速。
- **默写页**：英文单词大号居中，输入框紧贴下方，提交按钮屏幕宽度的 80%。进度条在最顶部。
- **手势支持**：学习卡片左右滑动切换，上滑标记“已掌握”。

#### PC 端布局（宽度 ≥ 768px）

- **经典三栏或两栏布局**：
  - 左侧可折叠侧边栏：单词列表，支持分组筛选、搜索、点击切换。显示每个单词的掌握情况（颜色点）。
  - 右侧主内容区：上方 Tab 切换“学习”“默写”，下方对应内容。
- **全局顶部栏**：设置（语速、自动播放等）、导入导出、关于。
- **键盘快捷键提示**：长按 `?` 键弹出快捷键列表（← → 切换、空格发音、Enter提交、1-3语速等）。
- **拖动调节**：单词列表和卡片区域可拖动分界线调整宽度。

### 5. 数据模型设计（与双端无关，但需健壮）

```javascript
wordData = {
  id: 'uuid',
  english: 'apple',
  chinese: ['苹果'],        // 数组支持同义多答
  group: 'default',
  correct: 0,              // 默写正确次数
  wrong: 0,                // 默写错误次数
  lastReview: 0,           // 时间戳
  learned: false           // 是否标记为已学
}

localStorage 结构:
{
  wordGroups: { default: [...], ielts: [...], custom: [...] },
  activeGroup: 'default',
  settings: { autoPlay: true, rate: 0.9, showChinese: false, fuzzyMatch: false },
  streak: { lastDate: '2026-07-25', count: 1 },
  wrongList: ['id1', 'id2'] // 当前错词ID列表
}
```

### 6. 技术方案（零后端架构，纯静态部署）

> **架构标准依据**：遵循技术架构文档第3节「技术栈选型」与第8节「构建与部署」标准，采用"构建产物为纯静态文件"的方案，确保可部署到任何静态托管服务。

- **前端框架**：Vue 3（组合式API）+ Ant Design Vue（按需导入），使用 Vite 构建工具。构建产物为纯静态 HTML/CSS/JS 文件，无后端依赖，可直接部署到 GitHub Pages、Vercel、Netlify 等静态托管服务。
- **响应式方案**：CSS Grid + Flexbox，利用媒体查询 `(min-width: 768px)` 切换布局。移动端禁用 hover 效果，PC 端增加细微动效。
- **状态管理**：使用 Pinia 进行全局状态管理，实现词库、学习进度、设置等数据的响应式同步。
- **路由管理**：Vue Router 实现单页应用路由切换，支持学习/默写/词库/统计页面的无刷新跳转。
- **发音引擎**：`window.speechSynthesis`，移动端需处理 `onvoiceschanged` 事件确保语音列表就绪。检测是否支持，不支持则显示降级提示。
- **离线与 PWA**：使用 Workbox 生成 Service Worker，采用"网络优先，缓存回退"策略缓存所有资源。配置 `manifest.json` 支持添加到主屏幕，实现离线学习。
- **触摸手势**：使用 `touchstart/touchend` 实现滑动，不引入 Hammer.js 等库以保持体积。
- **存储**：封装 `Storage` 模块，对 `localStorage` 的读写做 try-catch 防止存储满或隐私模式异常。所有数据仅存储在用户设备本地，零数据上传。

### 7. 发音功能双端特别处理

- **用户手势激活**：移动端浏览器通常会阻止自动播放音频，必须由用户手势触发首次 `speechSynthesis.speak()`。应用启动时显示一个覆盖按钮“开始学习”，点击后激活上下文并进入学习。
- **语音选择**：优先选择 `en-US` 语音，移动端和 PC 端可能提供不同的语音包，需枚举并挑选评分（`voice.voiceURI` 包含 "Google" 或 "Microsoft" 的选择自然度更好）。
- **降级方案**：如果 API 完全不可用（如部分老旧安卓 WebView），可准备少量核心单词的 base64 编码音频文件作为极简降级，但默认不考虑，仅提示。

### 8. 开发阶段规划（强调双端测试）

1. **基础架构与移动端优先**：搭建响应式骨架，完成移动端布局、底部导航、卡片学习与 TTS 发音。确保在 320px 宽度下可用。
2. **默写核心与 PC 适配**：实现默写逻辑、答案判定、错题记录。添加 PC 端键盘快捷键、双栏布局。适配 1920px 宽屏。
3. **数据管理与离线**：词库导入导出、分组、localStorage 持久化。注册 Service Worker，测试离线访问和添加到主屏幕。
4. **体验打磨**：手势滑动、动画反馈、打卡统计、难词本、设置面板。全面测试不同浏览器（Chrome, Safari, Firefox, Edge）。
5. **发布上线**：部署到 GitHub Pages / Vercel，配置 HTTPS，测试实际移动端访问。

### 9. 验收标准（双端）

- **移动端验收**（iPhone 12/Android 中等机型浏览器）：
  - 底部导航切换流畅，无布局错乱。
  - 滑动切换单词不卡顿，发音清晰。
  - 默写输入时键盘弹起页面不上移遮挡，能正常提交判分。
  - 离线模式（开启飞行模式）下仍可正常学习和默写，数据保存不丢失。
- **PC 端验收**（Chrome/Edge 1920\*1080）：
  - 左列表+右卡片的宽屏布局美观，信息密度适中。
  - 键盘方向键切换单词、空格发音、回车提交默写全部正常。
  - 拖拽 CSV 文件导入单词成功，导出 JSON 后能重新导入还原。
- **共通**：浏览器刷新后进度和设置仍保留。连续打卡天数正确计算。

### 10. 风险与边界

> **架构标准依据**：遵循技术架构文档第6节「安全策略」与第9节「性能优化」标准，补充零后端架构特有的风险点。

- **localStorage 容量限制**：一般 10MB，存储数千单词足够，但若导入大量词库需给出容量提示。
- **语音合成兼容性**：iOS Safari 有时 `onvoiceschanged` 触发延迟，需轮询获取语音列表。
- **离线缓存的更新**：Service Worker 需正确配置，避免旧版本缓存导致更新不生效，采用“网络优先，缓存回退”策略。
- **零后端数据安全**：所有数据存储在本地，用户清除浏览器数据将导致词库和进度丢失，需提供数据导出功能作为备份。
- **隐私模式限制**：部分浏览器隐私模式下 `localStorage` 不可用或受限，需检测并提供降级提示。
- **跨设备数据同步**：零后端架构下无法实现多设备数据同步，需引导用户通过导入导出功能手动迁移数据。
- **构建产物部署**：确保构建产物无后端依赖，避免代码中硬编码 API 地址或使用需后端支持的功能。

***

## 变更记录

| 变更位置      | 变更内容                                                                                  | 依据架构标准                      |
| --------- | ------------------------------------------------------------------------------------- | --------------------------- |
| 第5节 数据模型  | 增加 `badges` 字段到 `streak` 对象；添加零后端数据存储说明，明确前端 FileReader/Blob API 实现导入导出               | 技术架构文档第4节「数据模型」、第6节「安全策略」   |
| 第6节 技术方案  | 修正"无需构建工具"的错误描述，明确使用 Vite 构建；替换 Petite-Vue 为 Pinia；添加 Vue Router；明确使用 Workbox；强调零数据上传 | 技术架构文档第3节「技术栈选型」、第8节「构建与部署」 |
| 第8节 开发阶段  | 增加 Vite 构建验证步骤；明确前端 API 实现数据管理；强调静态托管部署验证                                             | 技术架构文档第8节「构建与部署」            |
| 第9节 验收标准  | 新增零后端架构验收条款，验证构建产物无后端依赖、可静态部署、无后端 API 请求                                              | 技术架构文档第10节「兼容性矩阵」           |
| 第10节 风险边界 | 新增零后端数据安全、隐私模式限制、跨设备同步、构建产物部署风险                                                       | 技术架构文档第6节「安全策略」、第9节「性能优化」   |

这份需求分析已将双端的差异性交互、布局、开发要点阐述清晰，可以直接进入 UI 设计和编码阶段。如果需要，我可以继续提供核心模块（如发音管理与默写判定）的代码实现。
